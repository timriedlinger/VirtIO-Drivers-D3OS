[package]
edition = "2024"
name = "vsock"
version = "0.1.0"

[lib]
crate-type = ["staticlib"]
path = "src/vsock.rs"

[dependencies]
# Lokale Abhängigkeiten
runtime = { path = "../../library/runtime" }
terminal = { path = "../../library/terminal" }
vsock = { path = "../../library/vsock" }